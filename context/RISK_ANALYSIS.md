# 🔍 تحليل المخاطر ومنطق العمل — Risk Analysis & Business Logic Audit

## 📋 فهرس التحليل
1. [مخاطر أمنية حرجة](#-مخاطر-أمنية-حرجة-critical)
2. [ثغرات منطق العمل](#-ثغرات-منطق-العمل-business-logic-bugs)
3. [مخاطر هيكلية ومعمارية](#-مخاطر-هيكلية-ومعمارية)
4. [مخاطر تجربة المستخدم](#-مخاطر-تجربة-المستخدم-ux)
5. [مخاطر الأداء](#-مخاطر-الأداء-performance)
6. [مخاطر سلامة البيانات](#-مخاطر-سلامة-البيانات-data-integrity)
7. [ميزات مفقودة/ناقصة](#-ميزات-مفقودةناقصة)

---

## 🚨 مخاطر أمنية حرجة (CRITICAL)

### SEC-01: قواعد Firestore مفتوحة بالكامل
| البند | التفصيل |
|-------|---------|
| **الملف** | `firestore.rules:25-27` |
| **المشكلة** | `isLocalOrAuthenticated()` تُرجع `true` دائماً |
| **التأثير** | **أي شخص يعرف مفاتيح Firebase يمكنه قراءة/كتابة/حذف كل البيانات** |
| **الخطورة** | 🔴 حرج — خسارة بيانات كاملة محتملة |
| **الحل** | إزالة `isLocalOrAuthenticated()` واستبدالها بـ `isAuthenticated()` مع ضمان عمل Anonymous Auth |

### SEC-02: كلمات المرور مخزنة كنص عادي
| البند | التفصيل |
|-------|---------|
| **الملف** | `AuthContext.tsx:80`, `Dashboard.tsx:1103-1105`, `Dashboard.tsx:1181` |
| **المشكلة** | كلمات المرور تُخزن وتُعرض كـ plaintext في Firestore وفي واجهة الإدارة |
| **التأثير** | أي وصول لقاعدة البيانات يكشف جميع كلمات المرور |
| **الخطورة** | 🔴 حرج |
| **الحل** | تشفير كلمات المرور (bcrypt/argon2) في طبقة خلفية، عدم عرضها في الجدول |

### SEC-03: مجموعة `staff` قابلة للقراءة العامة
| البند | التفصيل |
|-------|---------|
| **الملف** | `firestore.rules:83` — `allow read: if true;` |
| **المشكلة** | أي شخص يمكنه قراءة جميع أسماء المستخدمين وكلمات المرور |
| **التأثير** | اختراق كامل لجميع حسابات الموظفين |
| **الخطورة** | 🔴 حرج |
| **الحل** | نقل عملية التحقق إلى طبقة خلفية (Cloud Function أو Express API) |

### SEC-04: حساب مسؤول افتراضي مكشوف
| البند | التفصيل |
|-------|---------|
| **الملف** | `App.tsx:140-146` |
| **المشكلة** | يُنشأ تلقائياً حساب `admin`/`password123` في كل تحميل |
| **التأثير** | أي شخص يعرف بيانات الدخول الافتراضية يمكنه الدخول كمسؤول |
| **الخطورة** | 🟠 عالي |
| **الحل** | إزالة الـ seeding أو إضافة تحقق من وجود حسابات فعلية |

### SEC-05: إيميلات المسؤولين مكشوفة في الكود المصدر
| البند | التفصيل |
|-------|---------|
| **الملف** | `AuthContext.tsx:50`, `firestore.rules:31,40` |
| **المشكلة** | `aboodafaneh13@gmail.com`, `abd0175149@gmail.com` مكتوبة مباشرة (hardcoded) |
| **التأثير** | يمكن لأي شخص يقرأ الكود معرفة حسابات الأدمن |
| **الخطورة** | 🟡 متوسط |
| **الحل** | نقلها لمتغيرات بيئة أو Firestore config collection |

### SEC-06: مفاتيح Firebase مكشوفة في Git
| البند | التفصيل |
|-------|---------|
| **الملف** | `firebase-applet-config.json` |
| **المشكلة** | API Key ومعلومات المشروع في ملف مُتتبَّع في Git |
| **التأثير** | يمكن استخدام المفاتيح للوصول المباشر لخدمات Firebase |
| **الخطورة** | 🟡 متوسط (مع قواعد أمان صحيحة يكون أقل خطورة) |
| **الحل** | نقل الملف لـ `.env.local` أو إعداد domain restrictions على المفتاح |

### SEC-07: لا يوجد Rate Limiting على عمليات الدخول
| البند | التفصيل |
|-------|---------|
| **الملف** | `AuthContext.tsx:79-85` |
| **المشكلة** | يمكن تنفيذ عدد غير محدود من محاولات تسجيل الدخول (Brute Force) |
| **التأثير** | كشف كلمات المرور عبر المحاولات المتكررة |
| **الخطورة** | 🟠 عالي |
| **الحل** | إضافة حد أقصى لمحاولات الدخول الفاشلة + فترة حظر مؤقت |

### SEC-08: `localStorage` الجلسة غير مؤمنة
| البند | التفصيل |
|-------|---------|
| **الملف** | `AuthContext.tsx:125` |
| **المشكلة** | بيانات الجلسة مخزنة في `localStorage` بدون تشفير أو token expiry |
| **التأثير** | يمكن التلاعب بالجلسة عبر DevTools لتصعيد الصلاحيات |
| **الخطورة** | 🟠 عالي |
| **الحل** | استخدام httpOnly cookies أو على الأقل تشفير البيانات + TTL |

---

## 🐛 ثغرات منطق العمل (Business Logic Bugs)

### BL-01: زر "إضافة نشاط" غير موجود في واجهة الأنشطة
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:363-369` |
| **المشكلة** | مكوّن `ActivityForm` مُعرَّف (سطر 736) لكنه **غير مستخدم** في أي مكان |
| **التأثير** | **لا يمكن إنشاء أنشطة جديدة** ← المستخدم لا يستطيع استخدام النظام |
| **الخطورة** | 🔴 حرج — وظيفة أساسية معطلة |
| **الحل** | إضافة `<ActivityForm />` في header تبويب الأنشطة |

### BL-02: تعارض العملات (د.أ vs ₪)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:278-288` (د.أ) vs `Dashboard.tsx:1224,1245` (₪) |
| **المشكلة** | الأنشطة/الحجوزات/التكاليف تعرض بـ "د.أ" لكن التكاليف التأسيسية بـ "₪" |
| **التأثير** | ارتباك في البيانات المالية — تقارير غير دقيقة |
| **الخطورة** | 🟠 عالي |
| **الحل** | توحيد العملة أو إضافة نظام تحويل عملات واضح |

### BL-03: حذف التكاليف التأسيسية لا يعمل
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:1252` |
| **المشكلة** | زر الحذف موجود بصرياً لكن **بدون `onClick` handler** |
| **التأثير** | لا يمكن حذف تكاليف تأسيسية مُدخلة خطأً |
| **الخطورة** | 🟡 متوسط |
| **الحل** | إضافة handler: `onClick={() => deleteDoc(doc(db, 'foundationalCosts', c.id))}` |

### BL-04: لا يوجد تعديل على الأنشطة
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:363-369` |
| **المشكلة** | لا يمكن تعديل اسم/تاريخ/سعر/حالة أي نشاط بعد إنشائه |
| **التأثير** | أخطاء الإدخال غير قابلة للتصحيح |
| **الخطورة** | 🟠 عالي |
| **الحل** | إضافة نموذج تعديل (Edit Dialog) في بطاقة النشاط |

### BL-05: لا يوجد تعديل على الحجوزات
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:394-419` |
| **المشكلة** | يمكن فقط تأكيد الدفع أو الحذف — لا تعديل اسم/عدد/نشاط |
| **التأثير** | أي خطأ في إدخال حجز يتطلب حذفه وإعادة إنشائه |
| **الخطورة** | 🟡 متوسط |

### BL-06: لا يوجد حذف للتكاليف العادية
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:448-462` |
| **المشكلة** | جدول التكاليف لا يحتوي على أي أزرار إجراءات (حذف/تعديل) |
| **التأثير** | تكلفة مُدخلة خطأً تبقى إلى الأبد وتؤثر على الحسابات |
| **الخطورة** | 🟠 عالي |

### BL-07: حساب "تأكيد الدفع" يتجاهل المبلغ المدفوع الفعلي
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:413` |
| **المشكلة** | `paidAmount = basePrice * count` — يفترض دائماً أن المبلغ = السعر الكامل |
| **التأثير** | لا يدعم الخصومات أو الدفع الجزئي |
| **الخطورة** | 🟡 متوسط |
| **الحل** | إظهار Dialog لتأكيد/تعديل المبلغ المدفوع قبل الحفظ |

### BL-08: `toggleLayoutItem` و `updateNotificationSetting` يفشلان للموظفين
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:203-216` |
| **المشكلة** | الشرط `if (!user || !settings) return;` — الموظفون (`local_` sessions) ليس لديهم Firebase `user` |
| **التأثير** | الموظفون لا يملكون القدرة على تخصيص اللوحة أو الإشعارات |
| **الخطورة** | 🟡 متوسط |
| **الحل** | استخدام `user?.uid || profile.id` بدلاً من `user.uid` |

### BL-09: الإشعارات لا تصل لجميع المسؤولين
| البند | التفصيل |
|-------|---------|
| **الملف** | `notificationService.ts:31-38` |
| **المشكلة** | `notifyAllAdmins` فارغة تماماً (empty function) |
| **التأثير** | الإشعارات تصل فقط للمستخدم الذي نفّذ الإجراء (يُخطر نفسه) |
| **الخطورة** | 🟠 عالي |
| **الحل** | تنفيذ `notifyAllAdmins` عبر query لكل مستخدمي `users` collection |

### BL-10: عدم تصفية الأنشطة الملغاة/المكتملة في نموذج الحجز
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:861-863` |
| **المشكلة** | قائمة الأنشطة في نموذج الحجز تعرض **جميع** الأنشطة بما فيها الملغاة والمكتملة |
| **التأثير** | يمكن حجز مقعد في نشاط ملغي أو منتهي |
| **الخطورة** | 🟡 متوسط |
| **الحل** | تصفية: `activities.filter(a => a.status !== 'cancelled' && a.status !== 'completed')` |

### BL-11: مخطط الأرباح يعرض أول 5 فقط (بدون ترتيب مفيد)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:193-201` |
| **المشكلة** | `activities.slice(0, 5)` يأخذ أول 5 من المصفوفة المرتبة بـ date DESC |
| **التأثير** | يعرض آخر 5 أنشطة لكن العنوان يقول "آخر 5 أنشطة" — قد تكون مستقبلية |
| **الخطورة** | 🟢 منخفض |

---

## 🏗️ مخاطر هيكلية ومعمارية

### ARCH-01: ملف Dashboard أحادي ضخم (Monolith)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx` — **1357 سطر** |
| **المشكلة** | كل المكونات والمنطق والنماذج في ملف واحد |
| **التأثير** | صعوبة الصيانة، تعارضات Git، أداء IDE بطيء |
| **الخطورة** | 🟠 عالي (ديون تقنية متراكمة) |
| **الحل** | تقسيم لـ: `tabs/`, `forms/`, `components/`, `hooks/` |

### ARCH-02: لا يوجد طبقة خلفية (No API Layer)
| البند | التفصيل |
|-------|---------|
| **المشكلة** | الفرونت يتصل مباشرة بـ Firestore — كل المنطق في الكلاينت |
| **التأثير** | لا يمكن تنفيذ تشفير كلمات المرور، rate limiting، audit logs، أو أي منطق حساس server-side |
| **الخطورة** | 🟠 عالي |
| **الحل** | إضافة Express API endpoints للعمليات الحساسة (auth, staff management) |

### ARCH-03: عدم استخدام `useMemo` للحسابات المكلفة
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:167-201` |
| **المشكلة** | `totalRevenue`, `totalCosts`, `getActivityStats`, `upcomingActivities`, `chartData` تُحسب كلها في كل render |
| **التأثير** | ضغط على الأداء مع زيادة البيانات |
| **الخطورة** | 🟡 متوسط |
| **الحل** | تغليف بـ `useMemo(() => ..., [bookings, costs, activities])` |

### ARCH-04: 7 Real-time Listeners في وقت واحد
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:94-160` |
| **المشكلة** | 7 `onSnapshot` listeners مفتوحة دائماً بغض النظر عن التبويب النشط |
| **التأثير** | استهلاك bandwidth + Firestore reads حتى لو لم يكن المستخدم يشاهد البيانات |
| **الخطورة** | 🟡 متوسط |

### ARCH-05: لا يوجد Error Boundary
| البند | التفصيل |
|-------|---------|
| **المشكلة** | خطأ في أي مكوّن يُسقط التطبيق بالكامل (white screen) |
| **التأثير** | تجربة مستخدم سيئة + صعوبة تشخيص المشاكل |
| **الخطورة** | 🟡 متوسط |

---

## 🎨 مخاطر تجربة المستخدم (UX)

### UX-01: لا يوجد تأكيد قبل الحذف (في بعض الأماكن)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:415` |
| **المشكلة** | حذف الحجز يحدث مباشرة بنقرة واحدة — بدون تأكيد |
| **التأثير** | حذف بيانات بالخطأ لا يمكن استعادتها |
| **الحل** | إضافة `window.confirm()` أو Dialog تأكيد |

### UX-02: لا يوجد Pagination أو Search في الجداول
| البند | التفصيل |
|-------|---------|
| **المشكلة** | كل الحجوزات/التكاليف تُعرض دفعة واحدة |
| **التأثير** | بطء شديد مع +100 سجل — صعوبة إيجاد سجل معين |
| **الخطورة** | 🟠 عالي (مع نمو البيانات) |

### UX-03: لا يوجد تصفية/فلترة بالتاريخ أو النشاط
| البند | التفصيل |
|-------|---------|
| **المشكلة** | لا يمكن تصفية الحجوزات/التكاليف حسب النشاط أو الفترة الزمنية |
| **التأثير** | صعوبة إعداد تقارير مالية لفترة محددة |

### UX-04: حالة الأنشطة تُعرض بالإنجليزية
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:322` |
| **المشكلة** | `<Badge>{activity.status}</Badge>` يعرض `planned`/`active` بدلاً من الترجمة |
| **التأثير** | مظهر غير متسق في واجهة عربية |
| **ملاحظة** | الترجمة موجودة في `ActivityCard` (سطر 694-699) لكن غير مستخدمة في Overview |

### UX-05: لا يوجد Empty State لتبويب الأنشطة
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:363-369` |
| **المشكلة** | إذا لم تكن هناك أنشطة، يُعرض فراغ كامل |
| **التأثير** | المستخدم الجديد لا يعرف ماذا يفعل |

### UX-06: نموذج الحجز لا يُعاد تعيينه (Reset) بعد الإغلاق
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:806` |
| **المشكلة** | حالة `isFree` تبقى كما هي بعد إغلاق النموذج وإعادة فتحه |
| **التأثير** | الحجز التالي قد يسجل كمجاني بدون قصد |

### UX-07: زر تحميل الصورة (Camera) لا يعمل
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:1324-1326` |
| **المشكلة** | الزر موجود بصرياً لكن بدون أي functionality |
| **التأثير** | يوحي بميزة غير متوفرة — إحباط للمستخدم |

---

## ⚡ مخاطر الأداء (Performance)

### PERF-01: تحميل كل البيانات دفعة واحدة
| البند | التفصيل |
|-------|---------|
| **المشكلة** | لا يوجد pagination أو limit على queries — مع 10,000 حجز ستكون كارثة |
| **التأثير** | بطء شديد + تكلفة Firestore reads عالية |
| **الحل** | إضافة `limit()` + infinite scroll أو pagination |

### PERF-02: `getActivityStats` يُحسب O(n²)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:171-182`, `302`, `366` |
| **المشكلة** | يُستدعى لكل نشاط ← filter على كل bookings + costs في كل مرة |
| **التأثير** | 100 نشاط × 1000 حجز = 100,000 عملية فلترة في كل render |
| **الحل** | Pre-compute stats في `useMemo` كـ Map |

### PERF-03: `markAllAsRead` يُنفذ sequential updates
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:610-615` |
| **المشكلة** | `for` loop متسلسل — 50 إشعار = 50 طلب شبكي متتابع |
| **التأثير** | بطء ملحوظ عند تحديد كل الإشعارات كمقروءة |
| **الحل** | استخدام Firestore `writeBatch()` |

---

## 🗄️ مخاطر سلامة البيانات (Data Integrity)

### DI-01: لا Referential Integrity بين الحجوزات والأنشطة
| البند | التفصيل |
|-------|---------|
| **المشكلة** | حذف نشاط لا يحذف حجوزاته أو تكاليفه |
| **التأثير** | Orphaned bookings/costs تشوه الحسابات المالية |
| **الحل** | Cascade delete أو soft delete + validation |

### DI-02: لا يوجد تتبع تعديلات (Audit Trail)
| البند | التفصيل |
|-------|---------|
| **المشكلة** | لا يوجد سجل من عدّل/حذف/أنشأ أي سجل |
| **التأثير** | استحالة محاسبة الأخطاء أو التلاعب |

### DI-03: لا يوجد نسخ احتياطي مجدول
| البند | التفصيل |
|-------|---------|
| **المشكلة** | الاعتماد الكامل على Firestore بدون backup |
| **التأثير** | فقدان بيانات في حال حذف عرضي أو خلل |

### DI-04: عدم التحقق من تفرد اسم المستخدم (username)
| البند | التفصيل |
|-------|---------|
| **الملف** | `Dashboard.tsx:1103` |
| **المشكلة** | يمكن إنشاء موظفين بنفس اسم المستخدم |
| **التأثير** | `staffLogin` سيقبل أول نتيجة مطابقة — سلوك غير متوقع |

---

## 🚫 ميزات مفقودة/ناقصة

| # | الميزة | الأهمية | الوصف |
|---|--------|---------|-------|
| F-01 | تعديل الأنشطة | 🔴 حرج | لا يمكن تعديل أي نشاط بعد إنشائه |
| F-02 | حذف الأنشطة | 🟠 عالي | لا يوجد زر حذف في واجهة الأنشطة |
| F-03 | تعديل الحجوزات | 🟡 متوسط | لا يمكن تعديل بيانات حجز قائم |
| F-04 | حذف/تعديل التكاليف | 🟠 عالي | لا إجراءات على جدول التكاليف |
| F-05 | تعديل بيانات الموظفين | 🟡 متوسط | لا يمكن تعديل اسم/دور/كلمة مرور موظف |
| F-06 | البحث والفلترة | 🟠 عالي | لا يوجد بحث في أي جدول |
| F-07 | Pagination | 🟠 عالي | لا يوجد تصفح صفحات |
| F-08 | تصدير التقارير | 🟡 متوسط | لا يمكن تصدير بيانات مالية (PDF/Excel) |
| F-09 | تنبيهات اقتراب الموعد | 🟡 متوسط | تم ذكرها في الإعدادات لكن غير مُنفذة فعلياً |
| F-10 | تتبع حالة النشاط | 🟡 متوسط | لا يمكن تغيير حالة النشاط (planned → active → completed) |
| F-11 | تقارير مالية مفصلة | 🟡 متوسط | لا يوجد تقرير ربح/خسارة لكل نشاط |
| F-12 | إدارة الحجوزات لكل نشاط | 🟡 متوسط | لا يوجد عرض حجوزات نشاط معين |

---

## 📊 ملخص تصنيف المخاطر

| التصنيف | 🔴 حرج | 🟠 عالي | 🟡 متوسط | 🟢 منخفض |
|---------|--------|---------|----------|----------|
| **أمني** | 3 | 3 | 2 | 0 |
| **منطق عمل** | 2 | 3 | 5 | 1 |
| **هيكلي** | 0 | 2 | 3 | 0 |
| **UX** | 0 | 1 | 6 | 0 |
| **أداء** | 0 | 0 | 3 | 0 |
| **سلامة بيانات** | 0 | 0 | 4 | 0 |
| **الإجمالي** | **5** | **9** | **23** | **1** |
